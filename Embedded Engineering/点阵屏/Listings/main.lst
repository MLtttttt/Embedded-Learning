C51 COMPILER V9.54   MAIN                                                                  03/18/2026 22:08:27 PAGE 1   


C51 COMPILER V9.54, COMPILATION OF MODULE MAIN
OBJECT MODULE PLACED IN .\Objects\main.obj
COMPILER INVOKED BY: D:\Keil_v5\MDK\C51\BIN\C51.EXE main.c OPTIMIZE(8,SPEED) BROWSE DEBUG OBJECTEXTEND PRINT(.\Listings\
                    -main.lst) TABS(2) OBJECT(.\Objects\main.obj)

line level    source

   1          #include "reg52.h"
   2          
   3          sbit key0 = P1^0;   // 注意：原代码中是中文分号，已改为英文分号
   4          
   5          unsigned char code tab[]={
   6              0x00,0x7E,0xFF,0xC3,0xC3,0xFF,0x7E,0x00, //0
   7              0x00,0x00,0x43,0xFF,0xFF,0x03,0x00,0x00, //1
   8              0x00,0x63,0xC7,0xCF,0xDB,0xF3,0x63,0x00, //2
   9              0x00,0x42,0xDB,0xDB,0xDB,0xFF,0x66,0x00, //3
  10              0x00,0x3E,0x46,0xFF,0xFF,0x06,0x06,0x00, //4
  11              0x00,0xF6,0xFF,0xD3,0xD3,0xDF,0xDE,0x00, //5
  12              0x00,0x7E,0xFF,0xDB,0xDB,0xDF,0x4E,0x00, //6
  13              0x00,0xC0,0xC0,0xC7,0xFF,0xF8,0xC0,0x00, //7
  14              0x00,0xFF,0xFF,0xDB,0xDB,0xFF,0x7E,0x00, //8 
  15              0x00,0x72,0xFB,0xDB,0xDB,0xDF,0x7E,0x00, //9
  16          };
  17          
  18          unsigned char row[]={0x01, 0x02, 0x04, 0x08, 0x10, 0x20, 0x40, 0x80};
  19          unsigned char num = 0;
  20          
  21          // 简易延时函数（假设晶振12MHz，约1ms）
  22          void Delay1ms() {
  23   1          unsigned char i, j;
  24   1          for(i=2;i>0;i--)
  25   1              for(j=248;j>0;j--);
  26   1      }
  27          
  28          // 10ms延时用于按键去抖
  29          void Delay10ms() {
  30   1          unsigned char i, j;
  31   1          for(i=20;i>0;i--)
  32   1              for(j=248;j>0;j--);
  33   1      }
  34          
  35          // 按键处理：一次按下只加1，带去抖
  36          void key() {
  37   1          static bit press = 0;          // 静态变量记录是否已处理本次按下
  38   1          if(key0 == 0) {                // 检测到按下（低电平）
  39   2              if(press == 0) {           // 如果还未处理本次按下
  40   3                  Delay10ms();            // 延时去抖
  41   3                  if(key0 == 0) {         // 再次确认按下
  42   4                      num++;
  43   4                      if(num >= 10) num = 0;  // 限制0~9循环
  44   4                      press = 1;          // 标记已处理本次按下
  45   4                  }
  46   3              }
  47   2          } else {                        // 按键释放（高电平）
  48   2              press = 0;                  // 清除标记，准备下一次按下
  49   2          }
  50   1      }
  51          
  52          // 点阵扫描显示
  53          void matrix() {
  54   1          unsigned char i;
C51 COMPILER V9.54   MAIN                                                                  03/18/2026 22:08:27 PAGE 2   

  55   1          for(i = 0; i < 8; i++) {                // 扫描8行
  56   2              P3 = ~row[i];                         // 输出行选（取反，根据你的硬件可能是低电平有效）
  57   2              P2 = tab[8 * num + i];                 // 输出当前行的列数据
  58   2              Delay1ms();                            // 每行停留1ms，保证亮度均匀
  59   2          }
  60   1          // 可选：关闭所有显示，避免拖影
  61   1          // P3 = 0xFF;
  62   1          // P2 = 0x00;
  63   1      }
  64          
  65          void main() {
  66   1          while(1) {
  67   2              matrix();   // 不断刷新点阵
  68   2              key();      // 检测按键
  69   2          }
  70   1      }


MODULE INFORMATION:   STATIC OVERLAYABLE
   CODE SIZE        =    104    ----
   CONSTANT SIZE    =     80    ----
   XDATA SIZE       =   ----    ----
   PDATA SIZE       =   ----    ----
   DATA SIZE        =      9    ----
   IDATA SIZE       =   ----    ----
   BIT SIZE         =      1    ----
END OF MODULE INFORMATION.


C51 COMPILATION COMPLETE.  0 WARNING(S),  0 ERROR(S)
